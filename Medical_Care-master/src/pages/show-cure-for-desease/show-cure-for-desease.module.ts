import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { ShowCureForDeseasePage } from './show-cure-for-desease';

@NgModule({
  declarations: [
    ShowCureForDeseasePage,
  ],
  imports: [
    IonicPageModule.forChild(ShowCureForDeseasePage),
  ],
})
export class ShowCureForDeseasePageModule {}
